[pytest]
minversion = 7.0
addopts =
    -ra
    -q
    --strict-markers
    --cov=sql_mongo_converter
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
